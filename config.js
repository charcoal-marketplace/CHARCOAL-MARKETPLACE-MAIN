/* ==============
   API CONFIG 
================ */
const API_BASE = "/api";

/* =========================
   OPTIONAL HELPERS
========================= */
function getAPI(url) {
  return `${API_BASE}${url}`;
}